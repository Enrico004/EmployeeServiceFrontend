import { QualificationSortPipe } from './qualification-sort.pipe';

describe('QualificationSortPipe', () => {
  it('create an instance', () => {
    const pipe = new QualificationSortPipe();
    expect(pipe).toBeTruthy();
  });
});
