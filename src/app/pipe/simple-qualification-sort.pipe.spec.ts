import { SimpleQualificationSortPipe } from './simple-qualification-sort.pipe';

describe('SimpleQualificationSortPipe', () => {
  it('create an instance', () => {
    const pipe = new SimpleQualificationSortPipe();
    expect(pipe).toBeTruthy();
  });
});
