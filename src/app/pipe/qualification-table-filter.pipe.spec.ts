import { QualificationTableFilterPipe } from './qualification-table-filter.pipe';

describe('QualificationTableFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new QualificationTableFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
