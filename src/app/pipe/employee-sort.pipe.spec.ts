import { EmployeeSortPipe } from './employee-sort.pipe';

describe('EmployeeSortPipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeeSortPipe();
    expect(pipe).toBeTruthy();
  });
});
