import { ShortEmployeeSortPipe } from './short-employee-sort.pipe';

describe('ShortEmployeeSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortEmployeeSortPipe();
    expect(pipe).toBeTruthy();
  });
});
